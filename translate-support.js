const fs = require('fs'); let code = fs.readFileSync('app/[locale]/support-formation/support-formation-content.tsx', 'utf8'); code = code.replace('export function SupportFormationContent() {', 'import { useTranslations } from "next-intl";\nexport function SupportFormationContent() {\n  const t = useTranslations("Support");\n'); fs.writeFileSync('app/[locale]/support-formation/support-formation-content.tsx', code);
