const fs = require('fs'); let code = fs.readFileSync('components/layout/footer.tsx', 'utf8'); code = code.replace(/SMART2D Services accompagne les organisations dans la s.curisation,[\s\S]+?infrastructures critiques./, '{t("company_desc")}'); code = code.replace(/Expertise/g, '{t("expertise")}'); code = code.replace(/Nos partenaires/g, '{t("partners_title")}'); code = code.replace(/Banques, T.l.coms, Institutions publiques & priv.es, Entreprises technologiques et financi.res/g, '{t("partners_desc")}'); code = code.replace(/Solutions/g, '{t("solutions")}'); code = code.replace(/Entreprise/g, '{t("company")}'); code = code.replace(/Contact/g, '{t("contact")}'); code = code.replace(/Tous droits r.serv.s./g, '{t("rights")}'); code = code.replace(/Plus de 20 ans d&apos;expertise Oracle/g, '{t("years")}'); code = code.replace('export function Footer() {', 'import { useTranslations } from "next-intl";\nexport function Footer() {\n  const t = useTranslations("Footer");\n'); fs.writeFileSync('components/layout/footer.tsx', code);
